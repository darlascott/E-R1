void setup(){//setup function runs once
 size(2000,2000);//sets window size
 
}
void draw(){//runs forever
  if(mousePressed){//conditional
  background(random(255),random(255),random(255));
  delay(0);//pause a second ok
 }
}